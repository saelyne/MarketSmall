REST API

			    구현 여부			기능
User:

POST /user/register             X
POST /user/login 		X

Order:
POST /order/selectStore/	O			parameters: order_number, store_id, user_id, add row to order table with parameters
POST /order/addItem/		O			parameters: quantity, unitprice, req.body.sales_order_id,

Store:

GET /store/:id			O				Get list of materials of the store with :id